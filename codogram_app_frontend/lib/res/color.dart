import 'package:flutter/material.dart';

class AppColors {
  static const Color blackColor = Color(0xFF000000);
  static const Color whiteColor = Color(0xFFFFFFFF);
  static const Color buttonColor = Colors.green;

  // Gradient Colors (used in app bar title and story avatars)
  static const Color gradientPurple = Colors.purple;
  static const Color gradientBlue = Colors.blue;
  static const Color gradientPink = Colors.pink;

  // Feed card gradient colors
  static const Color feedGradientStart = Color(0xFF000000);
  static const Color feedGradientEnd = Color(0xFF2C3E50);

  // Text and border colors
  static const Color dividerColor = Colors.white;
  static const Color usernameTextColor = Colors.white;
  static const Color descriptionTextColor = Colors.white;
  static const Color tagTextColor = Color(0xFFD3D3D3); // Light grey
  static const Color postedDateColor = Color(0xFFB0B0B0); // Darker grey

  static const Color feedCardStart = Color(0xFF000000); // black
  static const Color feedCardEnd = Color(0xFF2C3E50); // dark blue
  static const Color navBarColor = Color(0xFF1A1A2E); // consistent with feed
  static const Color navButtonColor = Color(0xFFE91E63);
}
